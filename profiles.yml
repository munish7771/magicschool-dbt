magicschool:
  outputs:

    dev:
      type: postgres
      threads: 4
      host: 172.20.0.20
      port: 5432
      user: postgres
      pass: password
      dbname: my_app
      schema: dev

    prod:
      type: postgres
      threads: [1 or more]
      host: [host]
      port: [port]
      user: [prod_username]
      pass: [prod_password]
      dbname: [dbname]
      schema: [prod_schema]

  target: dev
